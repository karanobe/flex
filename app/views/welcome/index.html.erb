<h1>Flex</h1>

<% if user_signed_in? %>
  <p>link to gyms</p>
  <% if current_user.preference == nil %>
    <%= link_to "Preferences", new_preference_path %>
  <% else %>
    <%= link_to "Update Preferences", edit_preferences_path %>
    <p>link to search results</p>
  <% end %>
<% end %>
