<% if user == current_user %>
  <% if user.avatar %>
    <%= image_tag user.avatar.url(:square) %>
  <% end %>

  <h2> Welcome <%= user.first_name %> <%= user.last_name %></h2>
  <p>Your Age: <%= user.age %></p>
  <p>Home Gym: <%= user.gyms.name %></p>
  <p>Gender Pronoun: <%= user.gender_pronoun  %> </p>
  <p>Your current bio: <%= user.profile_bio %></p>

  <button>Edit Personal Information</button>
  <a href="<%= profile_load_path %>">Upload Profile Picture</a>

  <h3>Your flexmate preferences: </h3>
  <% if user.preference == nil %>
    <button href="/preferences/new" id='new-pref'>Add Preferences</button>
  <% else %>
  <p>Your prefered Flexmate's age range: <%= user.preference.min_age %> - <%= user.preference.max_age %></p>
  <p>Your prefered Flexmate's gender: <%= user.preference.gender %> </p>
    <button href="/preferences/<%= user.preference.id %>/edit" id='update-pref'>Edit Preferences</button>
  <% end %>
<% end %>


<% if user != current_user %>
  <% if user.avatar %>
    <%= image_tag user.avatar.url(:square) %>
  <% end %>
  <p><%= user.first_name %> <%= user.last_name %></p>
  <p>Age: <%= user.age %></p>
  <p>Home Gym: <%= user.gyms.name %></p>
  <p>Gender Pronoun: <%= user.gender_pronoun  %> </p>
  <p>Bio: <%= user.profile_bio %></p>

  <button>Send Friend Request</button>
<% end %>

<!-- if !(user == current_user) && current_user.friends.exclude?(user)  -->
<!-- add friend -->
<!-- elsif !(user == current_user) && current_user.friends.include?(user) -->
<!-- remove friend  -->
<!-- end  -->
